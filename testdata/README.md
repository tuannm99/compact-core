# ADD TEST DATA FOR COMPRESS LATER

<!-- REVIEWER [QUESTION][TESTING]: this change does not include the actual test fixtures or the compress tests that exercise them.
WHY: without the referenced test data and matching test cases, there is nothing concrete to verify for correctness, regression coverage, or compatibility of the compression flow.
FIX: include the intended fixture files and the tests that consume them, or provide the real code diff that uses this directory so the review can evaluate the behavior end to end.
-->
