# Python binding

Planned API:

```python

def encode_file(input, schema, output) -> None: ...
def decode_file(input, output) -> None: ...

```

This directory will wrap the `compact-ffi` C ABI in Phase 8.
